name: First simple pipeline

on:
  pull_request:
    branches: ["main"]
  workflow_dispatch:

jobs:
  first build action:
    runs-on: ubuntu-latest
    
    steps:
      - name: first step
        run: echo "This is the first step"
