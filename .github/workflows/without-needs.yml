# Shows two jobs, but the second job doesn't use the 'needs' keyword.

name: Without-needs
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - name: Build code
        run: |
          echo "Failing on purpose"
          exit 1
  
  # When needs is not specified, then both jobs run in parallel.
  # So, it makes sense that test succeeds if build fails, as they're independent.
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Test code
        run: |
          echo "simulating testing"

